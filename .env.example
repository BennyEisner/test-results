# ┌────────────────────────────────────────────────┐
# │        Sample environment configuration        │
# └────────────────────────────────────────────────┘

# PostgreSQL connection settings
DB_NAME=testresults
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Optional: Schema/Seed/Dump file locations (can be passed as CLI args instead)
# SCHEMA_FILE=sql/schema.sql
# SEED_FILE=sql/seed.sql
# DUMP_FILE=dump.sql
